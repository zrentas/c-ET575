// s. trowbridge 2024
#include <iostream>
using namespace std;

int main() {
    cout << endl;

    int x = 1;                  // x must be 1 for the loop to run                   

    while( x == 1 ) {           // infinite loop           
        cout << "Enter x: ";    // x never changes, loop runs forever
    }

    cout << endl;
    return 0;
}